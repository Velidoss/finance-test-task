import { makeStyles } from "@material-ui/core";

const RetrieveButtonStyle = makeStyles({
  stopButton: {
    height: 30
  },
  startButton: {
    height: 30
  },
});

export default RetrieveButtonStyle;