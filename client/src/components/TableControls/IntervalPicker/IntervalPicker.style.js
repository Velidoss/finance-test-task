import { createStyles, makeStyles } from "@material-ui/core";

const IntervalPickerStyles = makeStyles((theme) => createStyles({
  root: {
    color: 'rgba(111,123,233, 1)',
  },
}));

export default IntervalPickerStyles;