import { makeStyles } from "@material-ui/core";

const LoaderStyles = makeStyles({
  loaderContainer: {
    position: 'absolute',
    top: 0,
    left: 0,
    width: '100%',
    height: '100%',
  },
});

export default LoaderStyles;